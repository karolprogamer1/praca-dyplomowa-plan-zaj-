Uwaga: w tym repo wyszukiwarka/sortowanie robione lokalnie per komponent. 
Podejście: sortConfig (key+dir), filtered = base.filter(text), sorted = [...filtered].sort(compare).
Wykładowcy i przedmioty do uzupełnienia analogicznie do view_student/view_room.

